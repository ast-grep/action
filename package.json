{
  "name": "@ast-grep/github-action",
  "version": "1.0.0",
  "description": "A GH Action to run ast-grep rules",
  "main": "index.js",
  "scripts": {
    "build": "tsc && ncc build out/index.js --license LICENSE",
    "format": "prettier --write src",
    "check": "tsc --noEmit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/exec": "^1.1.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.0",
    "husky": "^9.0.0",
    "lint-staged": "^15.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.1.6"
  },
  "lint-staged": {
    "*": "prettier --write"
  }
}
